version: '3'
services:
  flask-app:
    build: .
    ports:
      - "8443:5000"
    # Thêm các cài đặt khác của ứng dụng Flask tại đây

  # proxy:
  #   image: nginx
  #   ports:
  #     - "8080:80"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf
  #   # Thêm cài đặt cấu hình nginx.conf tại đây